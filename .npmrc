; npm workspaces config
auto-install-peers=true
legacy-peer-deps=true
; Brug GitHub Packages for @bimo-nexus scope ved publish hvis NPM_TOKEN ikke er sat
; @bimo-nexus:registry=https://npm.pkg.github.com

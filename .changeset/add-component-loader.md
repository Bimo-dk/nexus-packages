---
'@bimo-dk/nexus-runtime': minor
---

Add `ComponentLoaderService` for loading arbitrary exposed modules from registered remotes at runtime. Enables advanced patterns like NgComponentOutlet grids, on-demand button-triggered loading, dialog-hosted components — anywhere you need to load a specific module beyond the auto-registered routes.

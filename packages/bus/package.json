{
  "name": "@bimo-dk/nexus-bus",
  "version": "1.0.0",
  "description": "Typed publish/subscribe + request/response event bus for Bimo-Nexus federated components",
  "license": "AGPL-3.0-or-later",
  "author": "Steffen Vitten Pedersen <svp@bimo.dk>",
  "repository": {
    "type": "git",
    "url": "https://github.com/Bimo-dk/nexus-packages.git",
    "directory": "packages/bus"
  },
  "scripts": {
    "build": "ng-packagr -p ng-package.json -c tsconfig.lib.prod.json",
    "test": "echo \"(bus tests via Angular TestBed in consumers)\" && exit 0",
    "lint": "echo \"(skipping lint — no eslint config yet)\" && exit 0",
    "type-check": "tsc --noEmit -p tsconfig.lib.json",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "tslib": "^2.3.0"
  },
  "peerDependencies": {
    "@angular/core": "^19.0.0",
    "rxjs": "^7.8.0"
  },
  "devDependencies": {
    "@angular/common": "^19.0.0",
    "@angular/compiler": "^19.0.0",
    "@angular/compiler-cli": "^19.0.0",
    "@angular/core": "^19.0.0",
    "ng-packagr": "^19.0.0",
    "rimraf": "^6.0.1",
    "rxjs": "^7.8.0",
    "typescript": "~5.6.3"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org",
    "directory": "dist"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
